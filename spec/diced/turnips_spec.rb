require 'spec_helper'

describe Diced::Turnips do
  it 'has a version number' do
    expect(Diced::Turnips::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
